function generateMatrix(n: number): number[][] {
    let newArr = new Array(n).fill(0).map((_v) => new Array(n).fill(0))
    let loop = Math.ceil(n / 2)
    let top = 0
    let left = 0
    let right = n - 1
    let bottom = n - 1
    while (loop--) {}

    return newArr
}
